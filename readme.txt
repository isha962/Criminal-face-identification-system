pip install mysql-connector-python==8.0.5
pip install tkintertable
pip install opencv-python

--install dblib and then install face-recognition --
--open cmd in this current directory path and then execute below command--

pip install (press tab, automatically u get dlib whl and then hit enter)

--then install below command in normal cmd (default c drive path)--

pip install face-recognition